먼저  이 프로젝트안에 클로드 엠디 파일을 만들어서    개발/ 수정시    다음의 규칙을 시행하기로 한다. 

1.  supabase 에  테이블을 생성할때,   테이블 이름앞에  "b-messenger_" 를 붙여서 생성한다. 

    예)    b-messenger_customers       ,       b-messenger_send       이런식으로 생성   
     
이유는 현재 supabase 데이터베이스 에    다른 테이블들이 정리가 되지 않아서..  앞에 b-messenger_ 입력하여 구분

2.  supabase  의  mcp 서버를 설치하여,  서버와 연결이 원할하게 테스트 후  진행하기로 한다.  

        클로드코드  mcp
------------------------------------

1{
2  "mcpServers": {
3    "supabase": {
4      "type": "http",
5      "url": "https://mcp.supabase.com/mcp?project_ref=lajjbrrysvkaxzrchanp"
6    }
7  }
8}
------------------------------------

# b-messenger (비지니스 메신져 발송 시스템) - Claude 규칙

## 포트 규칙
- **이 프로젝트 (b-messenger)는 항상 6666 포트 사용** (`npm run dev` → `next dev -p 6666`)
- farm-manager: 5555, BMS: 3333 사용 중이므로 절대 사용 금지

## 개발 서버 시작
```bash
npm run dev  # 자동으로 6666 포트 사용
```



## 작업 규칙
- 새 파일 생성 전 기존 파일 먼저 확인
- Supabase 환경변수는 `.env.local` 참조
- 컴포넌트는 `components/` 폴더에 정리

## 한국 시간대(KST) 규칙 ⭐
**모든 시간/날짜는 한국 시간(UTC+9)으로 처리**
- `next.config.ts`에 `process.env.TZ = "Asia/Seoul"` 설정됨
- 항상 `lib/utils.ts`의 한국 시간 함수 사용:
  - `getNowKST()` - 현재 한국 시간
  - `formatKSTDate()` - 포맷 형식 (기본값: 'YYYY-MM-DD HH:mm:ss')
  - `toKSTDateString()` - 날짜만 ('YYYY-MM-DD')
  - `formatKSTLocale()` - 한국식 포맷 ('2026. 3. 3. 오후 1:45:30')
  - `toKSTDate(date)` - Date 객체를 한국 시간으로 변환
- **절대금지**: `new Date().toUTCString()`, `new Date().toISOString()` (UTC 시간)

## 프로젝트 구분
- 🌾 **farm-manager** (포트: 5555) - 농장 관리 프로그램
- 📊 **BMS** (포트: 3333) - 건물/경영 관리 시스템
- 💬 **MSG System** (포트: 4444) - 고객 관리 메시지 발송 시스템

